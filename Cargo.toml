[package]
name = "tokio-proxy"
version = "0.1.0"
edition = "2021"
description = "A multi-interface proxy library built on Tokio."
license = "MIT"

[dependencies]
tokio = { version = "1.38", features = ["full"] }
anyhow = "1.0"
